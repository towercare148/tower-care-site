# Tower Care — הקשר פרויקט

## מי אני (המשתמש) ביחס לעסק
חבר של בעל העסק (עדי פדאל), לא הבעלים עצמו — עוזר להקים ולתפעל את האתר כפרוקסי טכני. עדי הוא אדם נפרד שמעביר לו פרטים אמיתיים (מחירים, היקף שירות, ביקורים בשטח). לא מפתח בעצמו — נוח עם ביצוע טכני עצמאי (git, דיפלוי, DNS), אבל מעדיף הסברים בשפה עסקית פשוטה על משמעות אירועים טכניים, לא ז'רגון עמוק.

## העסק
- שם: TOWER CARE (מותג). פרופיל Google Business נקרא "ניקיון חדרי מדרגות במודיעין והסביבה" (שם שונה, בכוונה — לא לשנות).
- בעלים: עדי פדאל, עוסק פטור, הוקם 2026.
- שירות: ניקיון ותחזוקה שוטפים לבנייני מגורים (לובי, חדר מדרגות, מעלית, קומות, חדר אשפה) במודיעין ו-14 ערים בסביבה.
- טלפון/וואטסאפ: 050-679-0612 (עדי). מייל עסקי: towercare148@gmail.com.
- דירוג גוגל: 5.0★, 30 ביקורות (מוצג באתר רק הציון, לא המספר — לבקשת הבעלים).
- **כלל ברזל: אף פעם לא להמציא נתונים עסקיים.** כל מספר/עובדה חייבים לבוא ישירות מהלקוח.
- אפיון מקורי מלא קיים במחשב ה-Windows המקורי בלבד: `C:\Users\ATID\Projects\tower-care\SPEC.md` (לא בריפו הזה — אם עובדים משם, אפשר לבקש את התוכן).
- 3 העדויות באתר (ינון דבוש, ליזי שמיר, פדאל) הן ביקורות אמיתיות מגוגל, הועתקו מילה במילה — לא לשכתב אותן.

## האתר
- חי ב: https://towercare.netlify.app (אין עדיין דומיין מותאם אישית — נדחה בכוונה).
- טכנולוגיה: TanStack Start (React, SSR) + Vite + Tailwind, דיפלוי דרך Netlify עם GitHub CI (פוש ל-main = דיפלוי אוטומטי).
- טופס לידים: שולח מייל בשקט דרך server function שקורא ל-Resend (מפתח ב-Netlify env var בשם `RESEND_API_KEY`, לא ב-git — יש `.env.example` כתבנית). **אין** פתיחת WhatsApp אוטומטית בשליחת טופס, ו**אין** תוכנית להתראות WhatsApp על לידים חדשים — נדחה במפורש על ידי הלקוח, לא להציע שוב.
- באג אמיתי שתוקן: תפריט מובייל היה מקונן בתוך `<header>` שמקבל `backdrop-blur`, מה ש"שבר" את מיקום ה-`fixed` overlay שלו (backdrop-filter יוצר containing block חדש). הפתרון: הוצאת התפריט להיות אח (sibling) של ה-header, לא ילד שלו.
- עמודים משפטיים אמיתיים (לא stub): `/privacy`, `/terms`, `/accessibility`.
- לפני שמכריזים שסיימתם: להריץ בפועל `npm run build`. אם זה ספציפי ל-SSR/Netlify, לשקול לבדוק גם את קובץ ה-function הבנוי בפועל דרך node (תפס באג production אמיתי פעם אחת ש-dev לא הראה).

## פריטים פתוחים
- **דומיין מותאם אישית** — לא נרכש עדיין בכוונה ("נתגלגל עם העסק קודם"). כשירכשו — לעדכן `siteUrl` ב-`src/routes/index.tsx` + DNS ב-Netlify.
- **מכסת build של Netlify** — עלולה להיגמר מריבוי פושים באותו יום (קרה פעם ב-13/08/2026, התאפסה 12/09). אם דיפלוי לא עולה — לבדוק את זה לפני שמניחים שיש באג בקוד.
- **קבצי הצעות מחיר ללקוחות** — נבנים כ-HTML עצמאי (עם תמונות מוטמעות base64) וממירים ל-PDF דרך Chrome headless. **חשוב:** קבצים כאלה הם **תמיד local-only** — אף פעם לא להעלות אותם לאתר החי (`towercare.netlify.app`) או לכל כתובת ציבורית/מאונדקסת בלי לשאול קודם. זו הייתה בקשת פרטיות מפורשת של הלקוח.

## איך לעבוד עם המשתמש הזה
- **לעולם לא להמציא נתונים** — אם חסר מספר/עובדה, לשאול.
- **לצמצם טוקנים** — נאמר כמה פעמים במפורש.
- **להציג אופציות ולחכות** לפני החלטות סובייקטיביות (ניסוח, עיצוב, מבנה) — לא לבחור לבד ולהריץ.
- **פעולה אוטונומית מרובת-שלבים בסדר גמור** ברגע שההחלטה סוכמה (התקנות, עריכת קבצים, build, git push).
